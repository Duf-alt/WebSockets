<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Ecommerce</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body { font-family: Arial, sans-serif; margin: 2rem; background: #f8f8f8; color: #333; }
    h1 { color: #222; }
    form { margin-bottom: 1rem; }
    input { padding: 0.4rem; margin-right: 0.5rem; }
    button { padding: 0.4rem 0.8rem; cursor: pointer; }
    ul { list-style: none; padding: 0; }
    li { margin-bottom: 0.5rem; background: #fff; padding: 0.5rem; border-radius: 6px; }
    nav a { margin-right: 1rem; text-decoration: none; color: #0077cc; }
  </style>
</head>
<body>
  <nav>
    <a href="/">🏠 Home</a>
    <a href="/realtimeproducts">⚡ Realtime Products</a>
  </nav>
  {{{body}}}
</body>
</html>
